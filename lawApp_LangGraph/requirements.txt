# LawAgent v2 (upgrade-v1) — LangChain 1.x stack
# Python 3.11

# ── Agent 框架 (1.x) ──
langchain>=1.3,<2
langchain-core>=1.4,<2
langchain-openai>=1.0,<2
langchain-community>=0.3.5
langchain-huggingface>=0.1.0
langchain-text-splitters>=1.0,<2
langgraph>=1.2,<2
langgraph-checkpoint-postgres>=3.1,<4

# ── LLM 追踪 ──
langsmith>=0.2

# ── Web 服务 ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.7.0

# ── 数据库 ──
psycopg[pool,binary]>=3.1
pgvector>=0.3.0
pinecone>=5.0.0
pinecone-text>=0.4.0

# ── 本地模型 (嵌入 + 重排序) ──
sentence-transformers>=3.0.0
tqdm>=4.66.0

# ── 输出与工具 ──
markdown>=3.7
pdfkit>=1.0.0
python-dotenv>=1.0.0
serpapi>=0.1.0              # langchain-community 0.4 的 SerpAPIWrapper 依赖新包名
